/// <reference types="@docusaurus/module-type-aliases" />

// Canonical @theme/* component types come from src/types/theme-classic.d.ts
// (H3603) — do not hand-declare @theme modules here; declaration merging
// would conflict with the upstream theme-classic declarations.
